# Template only - replace with real values (or a sealed-secret / external-secret)
# before applying. Do not commit real credentials.
apiVersion: v1
kind: Secret
metadata:
  name: bankdemo-db-credentials
  namespace: banco-kokunas
  labels:
    app.kubernetes.io/name: bankdemo-db
    app.kubernetes.io/part-of: banco-kokunas
type: Opaque
stringData:
  DB_NAME: bankdemo
  DB_USER: bankdemo
  DB_PASSWORD: changeme
